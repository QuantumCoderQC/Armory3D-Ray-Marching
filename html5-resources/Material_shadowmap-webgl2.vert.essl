#version 300 es

uniform mat4 W;
uniform mat4 LWVP;
uniform vec3 eye;
uniform float _time;

in vec4 pos;
out vec3 wposition;
out vec3 eyeDir;

void main()
{
    vec4 spos = vec4(pos.xyz, 1.0);
    wposition = vec4(W * spos).xyz;
    gl_Position = LWVP * spos;
    eyeDir = eye - wposition;
}

